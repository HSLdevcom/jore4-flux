#{{/* add comment that result yaml was generated */}} This is a generated file, all modifications will be overwritten!

# Patches the jore4 services to mount secrets as volumes
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jore4-hasura
  namespace: hsl-jore4
spec:
  template:
    spec:
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "jore4-hasura-secret-mapping"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jore4-auth
  namespace: hsl-jore4
spec:
  template:
    spec:
      volumes:
        - name: secrets-store-inline
          csi:
            driver: secrets-store.csi.k8s.io
            readOnly: true
            volumeAttributes:
              secretProviderClass: "jore4-auth-secret-mapping"
