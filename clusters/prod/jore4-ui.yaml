# This is a generated file, all modifications will be overwritten!

# Describes the default environment variables, secrets the deployment template and the service for jore4 UI instance
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: jore4-ui
  namespace: hsl-jore4
spec:
  replicas: 1
  selector:
    matchLabels:
      app: "jore4-ui"
  template:
    metadata:
      labels:
        app: "jore4-ui"
    spec:
      restartPolicy: Always
      containers:
        - name: "jore4-ui-image"
          image: "hsldevcom/jore4-ui:main--20220705-aa45ac4b9a8bbfc9d04a2f959fed8e4bb598eace"
          imagePullPolicy: Always
          ports:
          - containerPort: 80
          livenessProbe:
            httpGet:
              path: /
              port: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80


---
apiVersion: v1
kind: Service
metadata:
  name: jore4-ui
  namespace: hsl-jore4
spec:
  type: ClusterIP
  selector:
    app: jore4-ui
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80


